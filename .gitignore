# Training data (1.9 GB, downloaded at runtime)
tinystories.bin
input.txt

# Training outputs
samples.log
samples_*.log
loss.csv
checkpoints/

# Python
.venv/
__pycache__/
*.pyc

# Jetson-specific (not part of the public repo)
run.py
docker_run.sh
train_shakespeare.py
train_tinystories.py

# ESP32 build artifacts (weights.bin is regenerable via esp32/export_weights.py)
esp32/weights.bin
*.pt
